# MongoDB connection string
# This is already set in the main .env file - do not modify
MONGO_DB_STRING=mongodb+srv://username:password@cluster.mongodb.net/dbname

# Optional: Specify database name if different from the default
# If not specified, defaults to 'twitter_notifications'
DB_NAME=twitter_notifications

# Optional: Specify collection names if different from defaults
# If not specified, defaults to 'tweets'
TWEETS_COLLECTION=tweets